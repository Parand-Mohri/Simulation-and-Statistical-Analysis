% boxplot(data)
% x = zeros(length(data));
% scatter(data, x)
h = histogram(data,20);
[Y, E] = discretize(data, 20);
%data is normal with M mean and V variance 
M = mean(data);
st = std(data); 
p = normcdf(x,M,st);

chi = 0;
for i=1: length(E)
   chi = chi + E[]
end


